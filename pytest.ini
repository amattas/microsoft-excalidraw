[pytest]
testpaths = src
# icons dirs hold SVG/excalidraw data, not tests; some paths are unreadable in
# the Claude Code sandbox (*credentials*), which breaks collection if recursed.
norecursedirs = icons previews __pycache__
